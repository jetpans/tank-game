Actions = [FORWARD, BACKWARD, RIGHT, LEFT, FIRE,
            STOP_LINEAR, STOP_ANGULAR, GET_STATE]
MESSAGE FORMAT:

<action>,<tank_id> 
examples: 
FORWARD,0
RIGHT,1
STOP_ANGULAR,1

GET_STATE,x


SEND MESSAGE:
    message = "GET_STATE,0\n"
    client_socket.send(message.encode('utf-8'))

RECIEVE MESSAGE:
    data=client_socket.recv(1024)
    message = json.loads(data)